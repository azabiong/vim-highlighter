# Vim frequently used keywords

source so
if else elseif endif for endfor while endwhile break continue return finish try catch
function fu endfunction endf call
let unlet set setlocal setl
command com autocmd au augroup aug END

echo
